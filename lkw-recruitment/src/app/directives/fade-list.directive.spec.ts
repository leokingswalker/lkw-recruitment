import { FadeListDirective } from './fade-list.directive';

describe('FadeListDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeListDirective();
    expect(directive).toBeTruthy();
  });
});
